# تصميم موقع منظمة العمل الإنساني والتنمية المستدامة (HASDO)

توثيق شامل لأنظمة التصميم المستخدمة في الموقع — الألوان، الخطوط، التخطيط،
الخريطة التفاعلية، ولوحة البيانات. مكتوب للفريق والمطورين القادمين.

---

## 1. الهوية والعلامة

| العنصر | القيمة |
|---|---|
| الاسم الرسمي | منظمة العمل الإنساني والتنمية المستدامة |
| الاختصار | HASDO |
| الطراز | إنساني · تنموي · محلي |
| المصدر | ملف `Humanitarian Action and Sustainable LOGO.pdf` (الشعارات والألوان) |

### الشعار الأساسي
الشعار عبارة عن مربع أزرق (`#0E5CAD`) بحواف دائرية خفيفة، بداخله شكل تجريدي
أبيض يجمع بين **شجرة/ورقات** و**ثلاث أشخاص** بأذرع ممتدة — رمز يجمع بين
الاستدامة (الشجرة) والعمل الإنساني (الأشخاص). يوضع تحت الشعار اسم المنظمة
الكامل بخط عريض داكن باللون الأزرق نفسه، على سطرين:
"Humanitarian Action and Sustainable Development Organization (HASDO)".

**أصول الشعار** (في `public/img/`):
- `logo.png` — الشعار الرئيسي (مربع أزرق + الرمز الأبيض، كما في الملف الأصلي)
- `logo--white.png` — نسخة بيضاء (الرمز فقط) للاستخدام على خلفيات داكنة/الأزرق
- `logo-emblem.png` — الرمز وحده بدون النص، يُستخدم كفافيكون وشعار مصغّر
- `logo-multicolor.png` — النسخة متعددة الألوان (لصفحات الشراكات)
- `logo-variants.png` — ورقة الخيارات الكاملة من الملف المصدري

### الألوان الأساسية (من الشعار)
| اللون | HEX | الاستخدام |
|---|---|---|
| أزرق HASDO | `#0E5CAD` | اللون الأساسي / الشعار / الأزرار / روابط / تعبئة الخريطة |
| أخضر | `#0D8239` | شريط العلامة · مصادقات |
| وردي | `#EB1166` | شريط العلامة |
| ذهبي | `#EFA813` | لمسة التمييز (الخط تحت الكلمات، مؤشرات) |
| أحمر | `#EC132B` | إجراءات الحذف / التنبيهات |

### خط الواسم
- **Calisto MT** — خط شعار المنظمة في الـ PDF (ملكي، غير مثبت على الجهاز)،
  لا يُحمّل في الويب؛ يُستبدل في الواجهة بخط IBM Plex مماثل الروح.

---

## 2. نظام التصميم — "خفيف وراقٍ" (Light & Premium)

الطراز المعتمد بقرار من صاحب المشروع: **واجهات بيضاء فاتحة، طباعة كبيرة واثقة،
بطاقات ناعمة بحواف دائرية، ومساحات بيضاء سخية**، مع الأزرق العميق `#0E5CAD`
كعنصر جذب أساسي وألوان العلامة كشرائط صغيرة. استثناء وحيد لهذا الطراز هو
**الخريطة التفاعلية** (قسم 3)، التي تتبع هوية بصرية منفصلة مستوحاة من خرائط
المنظمات الإنسانية الدولية (نمط OCHA/UNFPA القُطري).

### المبادئ
1. **Mobile-First** — كود CSS يبدأ من الشاشات الصغيرة ويبني صعوداً باستعلامات
   `min-width`. أهداف اللمس ≥ 46px.
2. **التباين قبل الجمال** — النصوص دائماً داكنة على خلفيات فاتحة، وألوان
   الخريطة عميقة وواضحة (أزرق + أبيض) للقراءة الآمنة.
3. **البيانات حقيقة** — لا أرقام وهمية؛ تُعرض `—` حتى تُوثّق البيانات من لوحة التحكم.
4. **قاعدة بيانات واحدة** — الموقع بطيء القراءة من Supabase؛ كل ما يُسجَّل في
   اللوحة يظهر فوراً.

### لوحة الألوان (CSS variables)
```css
/* الطراز العام للموقع */
--bg:        #f6f8fb;   /* خلفية عامة فاتحة مائلة للزرقة */
--surface:   #ffffff;   /* سطوح البطاقات */
--ink:       #16283f;   /* نص أساسي */
--muted:     #5c6b7a;   /* نص ثانوي */
--line:      #e3e9f1;   /* حدود ناعمة */
--blue-700:  #0e5cad;   /* HASDO blue — الأساسي، لون الشعار */
--blue-100:  #e3eef8;   /* خلفيات فاتحة للاختيار/التمييز */
--brand-gold:#e8a213;   /* لمسة التمييز */

/* خاص بقسم الخريطة فقط — راجع القسم 3 */
--map-bg:      #4bc3d6; /* الخلفية الفيروزية/التركواز خلف القارة */
--map-active:  #0e5cad; /* الولايات/المحليات ذات الحضور — أزرق HASDO */
--map-inactive:#c9cfd6; /* الولايات بلا حضور — رمادي فاتح محايد */
--map-border:  #ffffff; /* حدود بيضاء عريضة بين المناطق */
```

### الخطوط
- **IBM Plex Sans Arabic** — النص العربي (أوزان 300–700).
- **IBM Plex Sans** — الأرقام والكلمات اللاتينية (`--font-latin`).
- تحميل عن طريق Google Fonts في `app/layout.tsx`.

### الأشكال والظلال
```css
--radius-sm: 10px;   /* حقول، شرائح */
--radius:    16px;   /* بطاقات */
--radius-lg: 22px;   /* إطار الخريطة، بطاقات تسجيل الدخول */
--shadow-sm: 0 1px 2px rgba(10,41,74,.05), 0 6px 18px rgba(10,41,74,.06);
--shadow-md: 0 2px 4px rgba(10,41,74,.05), 0 14px 34px rgba(10,41,74,.12);
```

### الشريط الخماسي (براند سترايب)
شريط بعرض 78px بألوان العلامة الخمسة (أخضر، وردي، أزرق، ذهبي، أحمر) —
يُستخدم كلمسة هوية في الهيرو والفوتر.

### الهيدر
- هيدر أبيض لاصق مع `backdrop-filter: blur(10px)`.
- على الهاتف: قائمة جانبية كاملة (Drawer) بزر قائمة؛ عناصر 50px بارتفاع الآن.
- على سطح المكتب: روابط دائرية (pill) بطول 999px.

### هيرو (الصفحة الرئيسية)
- خلفية فاتحة مع تدرّجات ناعمة بألوان الأزرق والذهبي (Radial gradients).
- عنوان ضخم `clamp(1.85rem → 2.6rem)`؛ الكلمة المحورية مميزة بخط ذهبي ناعم تحت النص.
- زرّان: أساسي متعبّد (أزرق + ظل)، وثانوي بحواف.
- في الهاتف: الأزرار بعرض كامل تصفّف رأسياً.

### القصاصات (Sections)
- `--alt` أسطح بيضاء بحدود علوية/سفلية رفيعة للفصل.
- العناوين كبير بعنوان `3xl` وسطر قبلي (kicker) بأسلوب لاتيني صغير.
- البطاقات تعطى ظلاً ناعماً وارتفاعاً خفيفاً عند التمرير.

---

## 3. الخريطة التفاعلية

تغيّر الطراز البصري للخريطة ليطابق النمط المرجعي المعتمد (خرائط الحضور
القُطري لدى منظمات مثل UNFPA/OCHA): **خلفية فيروزية ثابتة، مناطق مصمَتة
بالأزرق لا تفتيح/تعتيم عند التمرير، أسماء الولايات مكتوبة مباشرة داخل الشكل
بالأبيض العريض، ومفتاح (Legend) واضح أسفل يسار الخريطة**.

### المصادر
- **CB (COD-AB / OCHA) عبر Humanitarian Data Exchange** بملف `scripts/prepare-map.mjs`:
  - `public/data/sudan-states.json` — 18 ولاية رسمية (كود `SD01`–`SD18`).
  - `public/data/sdn-khartoum-localities.json` — 7 محليات داخل الخرطوم.
- تحسين اسم محافظة: البيانات تأتي بـ `Aj Jazirah` (وليس `Al Jazirah`).

### المحليات (خرطوم)
| محلية | الكود |
|---|---|
| بحري | SD01003 |
| جبل أولياء | SD01001 |
| كرري | SD01005 |
| الخرطوم | SD01007 |
| شرق النيل | SD01004 |
| أمبدة | SD01002 |
| أم درمان | SD01006 |

### النمط البصري الجديد
- **الخلفية**: لون فيروزي/تركواز ثابت `--map-bg` (`#4bc3d6`) يملأ لوحة SVG
  بالكامل — لا أبيض ولا تدرّج، بعكس بقية الموقع.
- **الولايات ذات الحضور**: تُملأ باللون الأزرق `--map-active` (`#0e5cad`)
  بشكل دائم (وليس عند التمرير فقط)، مع اسم الولاية مكتوباً داخلها مباشرة
  بخط أبيض عريض واضح.
- **الولايات بلا حضور**: رمادي فاتح محايد `--map-inactive` (`#c9cfd6`)، بلا
  نص ظاهر، لتقليل التشتيت مع بقاء حدودها مرسومة.
- **الحدود**: خط أبيض عريض `--map-border` يفصل كل منطقة عن جاراتها وعن خلفية
  البحر/الدول المجاورة — يمنح تأثير "قصّ ولصق" واضح كما في المرجع.
- **مؤشرات الوجود (نقاط)**: نقطتان تُرسمان فوق مركز كل منطقة نشطة:
  - **دائرة معبّأة** (●) بيضاء صغيرة = **المقر الرئيسي الوحيد** (Headquarters).
    مقر واحد فقط في السودان: الخرطوم — لا فروع ثانوية.
  - **دائرة مفرغة** (○) بيضاء الحد = **مكان عمل بلا مكتب**
    (Where we work).
- **بدون تأثير تمرير مُعتّم**: يُلغى سلوك "تعتيم بقية المناطق 0.3" المعتمد
  سابقاً؛ التصميم الجديد ثابت الألوان دائماً، والتفاعل (Click/Tap) يفتح لوحة
  التفاصيل الجانبية دون تغيير تعبئة بقية الخريطة، حفاظاً على الوضوح الكامل
  لكل الولايات في آن واحد كما في المرجع.

### المفتاح (Legend)
صندوق أبيض صغير بزوايا دائرية وظل خفيف (`--shadow-sm`)، يُثبَّت في **الزاوية
اليسرى السفلية** من إطار الخريطة (على الشاشات الصغيرة: أسفل الخريطة كشريط
كامل العرض)، ويحتوي سطرين فقط:
```
●  المقر الرئيسي            (Headquarters — الخرطوم)
○  حيث نعمل                (Where we work)
```

### السلوك التفاعلي
- اختيار/الضغط على ولاية نشطة → لوحة تفاصيل (المشاريع، المستفيدون، المجتمعات)
  تنزلق من الجانب، بينما تبقى الخريطة نفسها ثابتة التلوين كما هي.
- اختيار **الخرطوم** (SD01) → تحوّل الخريطة إلى عرض المحليات السبع بنفس
  النمط البصري (فيروزي/أزرق/رمادي + حدود بيضاء) — تعمّق داخلي بلا فقدان الهوية.
- على الهاتف: قائمة الولايات في عنصر `details` قابل للطي («ولايات السودان ▾»)
  حتى لا تزحم الصفحة؛ وعلى سطح المكتب تظهر القائمة كاملة بجانب الخريطة.

### ملاحظة تقنية مهمة (Hydration)
ميّزات d3-geo (مركز المضلع `centroid`، ومواضع الدوائر فوق كل منطقة) ليست
متطابقة البتات بين Node والمتصفح، ما سبّب خطأ Hydration سابقاً. الحل: **قياس
كل الإحداثيات إلى خانتين عشريتين** بغلاف `geoTransform` داخل
`components/SudanMap.tsx`، فينتج الخادوم والمتصفح قيم متطابقة 100%.
(تحقق: 0 إحداثيات طويلة على الصفحة.)

---

## 4. الصفحات

| المسار | المحتوى |
|---|---|
| `/` | هيرو، خريطة تفاعلية، المجالات الثلاثة، أثر بالأرقام، قصص، مشاركة |
| `/about` | من نحن، رؤيتنا، منهجيتنا |
| `/areas` | مجالات العمل + بنية بيانات المشروع |
| `/map` | خريطة أين نعمل + مصدر البيانات (OCHA) |
| `/impact` | المؤشرات، مراحل المشاريع، جدول المشاريع |
| `/projects` | قائمة المشاريع ببطاقات |
| `/projects/[slug]` | صفحة مشروع: أرقام، هدف، أثر، تفاصيل |
| `/field` | قصص من الميدان (تُدار من اللوحة) |
| `/resources` | الموارد + الشركاء |
| `/participate` | طرق المشاركة |

**مكونات مشتركة:** `PageHero` (ترويسة صفحات داخلية)، `SubNav` (تنقل لاصق)،
`Reveal` (ظهور عند التمرير يحترم تقليل الحركة)، `PublicHeader/PublicFooter`.

---

## 5. التصميم لكل صفحة (Page Layouts v2)

نظام صفحة-بصفحة يحدد بنية كل صفحة بدقة — الشبكة، الطباعة، ترتيب الأقسام،
والحركة — بهدف **الخروج من قوالب مواقع المنظمات غير الربحية النمطية** (hero
بصورة ستوك + 3 بطاقات أيقونة + شريط إحصاءات دائري + عداد داعمين + كاروسيل
شهادات). كل قرار هنا مبني على هوية HASDO الفعلية: الشريط الخماسي، منطق
الخريطة (نقطة معبأة/مفرغة)، والطابع "التقرير الميداني" بدل الطابع "الخيري
العام".

### المبادئ المانعة (Anti-Slop Rules)

قبل أي صفحة — هذه ممنوعة في كل الموقع:
- ❌ Hero بخلفية "blob" متدرجة ضبابية بنفسجي/أزرق مع نص في المنتصف تماماً.
- ❌ صفوف من 3 أو 4 بطاقات متطابقة (أيقونة دائرية + عنوان + سطرين) بمسافات متساوية.
- ❌ عدّادات إحصائية أفقية "12,000+ مستفيد" بدون سياق مكاني/زمني.
- ❌ كاروسيل شهادات بصور دائرية وعلامات اقتباس كبيرة.
- ❌ صور ستوك لأشخاص يبتسمون بشكل عام دون سياق حقيقي.
- ❌ أزرار بيضاوية بتدرّج لوني وظل ناعم موحّد يتكرر 15 مرة في الصفحة.
- ❌ تمركز كل شيء عمودياً/أفقياً بلا تسلسل هرمي — كل شيء بنفس الوزن البصري.
- ❌ خط Inter/Poppins افتراضي بلا شخصية.

البديل الدائم: **شبكة كسورة (broken grid)** — أعمدة غير متساوية العرض
عمْداً، بيانات حقيقية بدل الزخرفة، ولون يُستخدم كوظيفة (حالة/نوع) لا كديكور.

### النظام الشامل (يُطبَّق عبر كل الصفحات)

**الشبكة** — شبكة 12 عموداً على سطح المكتب (`max-width: 1360px`)، لكنها
**لا تُستخدم بتناظر 6/6 أو 4/4/4 أبداً** — كل صفحة تكسر التناظر عمْداً (مثال:
عمود نص 7 + عمود بيانات جانبي 5، أو 8/4، أو صورة تتجاوز حدود الحاوية بينما
النص محصور). وحدة القياس الأساسية `8px`؛ الهوامش بين الأقسام الكبرى
`96–140px` على سطح المكتب و`56–72px` على الهاتف — فراغ كبير مقصود بين
الأقسام، لا داخلها.

**سلّم الطباعة (نسبة 1.333 — Perfect Fourth من أساس 17px):**
| المستوى | الحجم (سطح مكتب) | الاستخدام |
|---|---|---|
| Display | `clamp(2.6rem, 5vw, 4.2rem)` | عنوان الهيرو فقط — سطر أو سطران كحد أقصى |
| H1 | `2.4rem` | عنوان الصفحة الداخلية |
| H2 | `1.8rem` | عنوان قسم |
| Kicker | `0.8rem` — لاتيني، `letter-spacing: .12em`, uppercase | سطر تمهيدي فوق كل H2 |
| Body | `1.05rem` / line-height `1.7` | فقرات |
| Data/Numeral | IBM Plex Sans (لاتيني)، `font-variant-numeric: tabular-nums` | كل رقم إحصائي |

**قاعدة إلزامية**: لا يظهر عنوان Display إلا مرة واحدة في الصفحة كلها —
لمنع "تسطيح" الأهمية.

**لغة الأيقونة/النقطة (Pin Language)** — النقطة المعبأة (●) والمفرغة (○)
من الخريطة (قسم 3) تصبح **نظام إشارة بصرياً عاماً** بدل الأيقونات العامة
(نجمة، قلب، يد): ● = نشط/مكتمل/رسمي، ○ = مخطط/محتمل/غير مباشر. تظهر بجانب:
حالة مشروع، نوع مكتب، فئة مورد، مرحلة في الجدول الزمني.

**الحركة** — لا حركة زخرفية (لا "fade up" لكل عنصر عند التمرير). الوحيدة
المسموحة: **رسم الخريطة** (المناطق "تُبنى" بترتيب جغرافي عند أول تحميل،
400ms)، و**عدّاد الأرقام** من 0 إلى قيمته الحقيقية مرة واحدة فقط عند أول
دخول في إطار الرؤية. الانتقال بين الصفحات: `opacity` خفيف 120ms فقط، لا
انتقال كامل الشاشة.

**التصوير** — لا صور ستوك عامة. البدائل بالترتيب: (1) رسوم بيانية من
بيانات حقيقية (أعمدة، خرائط مصغّرة، جداول)، (2) صور ميدانية فعلية موثّقة
بمصدر/تاريخ/موقع مكتوب تحتا دائماً، (3) عند غياب الاثنين: **لا صورة
إطلاقاً** — نمط هندسي بسيط من ألوان العلامة (خطوط رفيعة، لا أشكال "blob").

### `/` الصفحة الرئيسية

**الفرضية**: الزائر يرى "أين نعمل" قبل أن يقرأ جملة تعريفية واحدة — الجغرافيا
تقود، لا الشعار الخطابي.

1. **الهيرو (بلا شاشة كاملة قياسية)** — شبكة 7/5 غير متماثلة:
   - العمود الأيمن (7): عنوان Display بسطرين كحد أقصى + Kicker فوقه + زرّان
     (أساسي/ثانوي) + شريط ثقة صغير أسفل الأزرار (عدد الولايات + عدد المشاريع
     النشطة كنص، لا بطاقات).
   - العمود الأيسر (5): **الخريطة التفاعلية الحقيقية مصغّرة** (ليست صورة) —
     تفاعلية من أول ثانية، بخلفيتها الفيروزية تكسر الأبيض المحيط فوراً. هذا
     أقوى عنصر بصري في الصفحة، لا صورة بطل عامة.
   - لا خلفية متدرجة خلف النص؛ خلفية بيضاء نظيفة + الشريط الخماسي كخط رفيع
     8px أسفل الهيرو مباشرة يفصله عمّا يليه.
2. **مجالات العمل** — ليست 3 بطاقات متطابقة. تخطيط "قائمة مرقّمة أفقية ضخمة":
   كل مجال سطر كامل العرض برقم كبير جداً (01/02/03) بخط رفيع شفاف جزئياً،
   عنوان المجال بجانبه، ووصف قصير على عمود محدود العرض (لا كامل السطر).
3. **الأثر بالأرقام كمفتاح خريطة** — بدل صف عدّادات: صندوق واحد يشبه Legend
   الخريطة تماماً (أبيض، ظل ناعم) فيه 4 أسطر فقط: نقطة ملوّنة + رقم tabular
   + تسمية — ربط بصري مباشر بمفتاح الخريطة الذي رآه الزائر في الهيرو.
4. **قصص من الميدان** — قصة واحدة كبيرة (صورة + نص تحريري طويل نسبياً)
   بجانبها عمود ضيق لقصتين أصغر نصياً فقط بلا صور — لا شبكة متساوية من 3 بطاقات.
5. **الشركاء** — شريط أسماء نصي بسيط (لا شعارات رمادية) بخط لاتيني متوسط،
   يمر أفقياً تلقائياً خفيفاً فقط عند وجود أكثر من 8 شركاء.
6. **إغلاق/مشاركة** — شريط عريض بخلفية `--blue-700` (اللون الوحيد الذي
   يُستخدم كخلفية كاملة في الصفحة)، نص أبيض، زر واحد فقط.

### `/about`

تخطيط تحريري بعمودين **غير متساويين غير لاصقين بالكامل**:
- العمود الأيمن (8): نص طويل بعناوين H2 صغيرة، فقرات لا تتجاوز 70 حرفاً
  (`max-width: 62ch`) لسهولة القراءة.
- العمود الأيسر (4): **3 اقتباسات/أرقام متباعدة رأسياً** تظهر عند محاذاة
  التمرير للفقرة المرتبطة بها فقط (ثبات نسبي لا مطلق) — أرقام مثل "تأسست
  20xx" أو "تعمل في X ولاية" بحجم كبير جداً كعنصر طباعي لا داخل بطاقة.

**الجدول الزمني**: بدل خط عمودي بنقاط دائرية — **الشريط الخماسي نفسه ممتد
أفقياً كخط زمني**، كل حدث نقطة على الشريط بلونه المطابق لمرحلته (تأسيس=أزرق،
توسّع=أخضر، شراكة=وردي...)، تسميات تعلو وتحت الخط بالتناوب.

### `/areas`

كل مجال عمل = شريط كامل العرض **يتناوب اتجاهه يميناً/يساراً**:
- نص (عنوان + وصف + رابط "اطّلع على المشاريع") في عمود 5.
- عمود 7: ليس صورة — **رسم بياني شريطي أفقي بسيط** (D3/SVG، لا مكتبة رسوم
  ثقيلة) يوضح توزيع المستفيدين/المشاريع في المجال عبر الولايات، بلون المجال.
- خلفية الشريط تتبدل بين `--surface` و`--bg` بالتناوب فقط (لا ألوان زاهية).

### `/map`

- الخريطة تملأ العرض بالكامل بارتفاع `calc(100vh - header)` عند أول تحميل —
  لا عنوان ضخم يزاحمها؛ شريط رفيع علوي شفاف بعنوان صغير + Legend ثابت أسفل
  اليسار كما في الملف الرئيسي.
- عند اختيار ولاية: لوحة تفاصيل **لا تُفتح كـ Modal مركزي** — تنزلق من
  الحافة اليمنى بعرض 380px فقط، تاركة الخريطة مرئية خلفها (شفافية 6%) —
  الجغرافيا تبقى سياقاً دائماً.
- أسفل ثنية الشاشة: **جدول بيانات كامل** (لا شبكة بطاقات) بكل الولايات/
  المحليات — الاسم، الحالة (●/○)، عدد المشاريع، آخر تحديث — للإتاحة والفهرسة.

### `/impact`

تخطيط "لوحة تحكم" فعلي:
1. **شريط KPI علوي**: 4 قيم كنص tabular كبير بمحاذاة يسار (لا بطاقات بحدود)
   مفصولة بخطوط رأسية رفيعة فقط `--line`.
2. **مسار المراحل** (مُخطط → نشط → منفّذ): ثلاثة أعمدة **بعرض غير متساوٍ
   يتناسب فعلياً مع عدد المشاريع** في كل مرحلة — عرض العمود نفسه بيانات.
3. **جدول المشاريع**: `<table>` حقيقي بترويسة لاصقة، قابل للفرز بالنقر على
   العناوين.

### `/projects`

- شبكة بطاقات **غير موحّدة الارتفاع** (Masonry خفيف حسب طول العنوان الفعلي،
  لا ارتفاع ثابت يقطع النصوص بثلاث نقاط دائماً).
- كل بطاقة: نقطة حالة ملوّنة أعلى اليسار (تربطها بلون ولايتها في الخريطة)،
  اسم المشروع بخط كبير نسبياً ("نصية القيادة" لا صورة القيادة)، وإن وُجدت
  صورة ميدانية حقيقية فهي **أسفل النص لا أعلاه** — عكس ترتيب قوالب NGO.
- تصفية (Filter) بالمجال/الولاية كأزرار نصية تحت خط، لا قائمة منسدلة ولا
  Chips كثيرة.

### `/projects/[slug]`

- **الهيرو خريطة لا صورة**: شكل الولاية/المحلية الخاصة بالمشروع مكبّراً
  ومفرداً (نفس بيانات GeoJSON)، بلون الحالة، يملأ ثلث الشاشة العلوي — كل
  صفحة مشروع تبدأ بـ"أين" لا بـ"كيف يبدو المكان".
- تحتها مباشرة: شريط أرقام أفقي (الهدف/المستفيدون/الميزانية إن وُجدت) بخط
  tabular كبير، بلا أيقونات.
- المحتوى: عمود نصي واحد فقط (لا عمودين) بعرض قراءة مريح — تُقرأ كتقرير حالة
  لا كصفحة تسويق منتج.
- الشركاء/الصور الميدانية (إن وُجدت) في شريط أفقي منخفض أسفل الصفحة، لا
  كاروسيل رئيسي.

### `/field`

تخطيط صحفي حقيقي:
- قصة رئيسية: عرض كامل، عنوان كبير جداً بخط تحريري، تاريخ + موقع مكتوبين
  بخط Kicker فوق العنوان (لا اسم كاتب عام).
- بقية القصص: عمودان فقط (ليس 3)، نص أطول من المعتاد — سطران إلى ثلاثة من
  المعاينة بدل جملة مقتطعة.
- لا صور دائرية، لا أيقونات مشاركة اجتماعية عائمة.

### `/resources`

- قائمة (لا شبكة بطاقات ملفات بأيقونة PDF حمراء متكررة). كل صف: نقطة ملوّنة
  حسب الفئة + اسم المستند + نوع الملف كنص صغير + زر تحميل نصي على اليمين —
  جدول بيانات فعلياً، مطابق لروح `/impact`.
- تجميع حسب الفئة بعناوين H2 صغيرة، لا تبويبات (Tabs) تخفي المحتوى.

### `/participate`

ثلاث كتل كاملة العرض متتالية (تطوّع / تبرّع / شراكة)، كل كتلة:
- خلفية متبادلة بلون علامة فاتح جداً (5% شفافية) مختلف لكل كتلة — تمييز
  الأنواع الثلاثة بالهوية اللونية القائمة بدل أيقونات جديدة.
- عنوان + وصف موجز + **زر واحد فقط** يقود لنموذج/بريد مخصص لذلك المسار
  تحديداً — لا نموذج "اتصل بنا" عام واحد.

### `/admin/*`

يبقى **عمداً غير مطابق** لهذه الهوية — واجهة كثيفة وظيفية (جداول، نماذج
قياسية، بلا شريط خماسي ولا خريطة فيروزية) لتبقى الهوية البصرية الغنية حصراً
للواجهة العامة، ولتقليل وقت بناء اللوحة وصيانتها.

### ملخص: أين يظهر كل عنصر هوية عبر الموقع

| العنصر | الصفحات |
|---|---|
| الخريطة الحية/المصغّرة | الرئيسية (هيرو)، `/map`، صفحة المشروع (هيرو مصغّر)، الفوتر (نسخة ثابتة) |
| الشريط الخماسي | فاصل تحت الهيرو الرئيسي، خط تحت رابط الهيدر النشط، الجدول الزمني في `/about`، أعلى الفوتر |
| نقطة ●/○ | بطاقات المشاريع، جدول `/impact` و`/map`، مفتاح الخريطة، قائمة `/resources` |
| أرقام Tabular كبيرة | مفتاح "الأثر بالأرقام"، `/impact` KPIs، `/about` sidebar، صفحة المشروع |
| لا صور ستوك | في كل الموقع دون استثناء — فقط بيانات أو صور ميدانية موثّقة |

---

## 6. لوحة البيانات (المسؤول)

- العنوان: `/admin/login` ثم `/admin/overview`.
- **الحماية**: ميديوير يوجه `/admin/*` لصفحة الدخول عند غياب الجلسة؛ القراءة
  العامة عبر RLS، والكتابة حصرياً عبر مفتاح `service_role` في `Server Actions`.
- الأقسام:
  - نظرة عامة (KPIs) — مشاريع، نشطة، منفذة، مستفيدون، قصص، موارد، شركاء، ولايات.
  - المشاريع — إنشاء/تعديل/حذف (اسم، رابط slug، ولاية/محلية، مجالات، حالة،
    تواريخ، أهداف، أرقام، شركاء، صور).
  - القصص والأخبار — نشر مباشر يظهر في الرئيسية و«من الميدان».
  - الموارد — مستندات بروابط تحميل.
  - الشركاء — اسم، دور، موقع.
  - الإعدادات — مؤشرات إضافية، واختيار **المقر الرئيسي الوحيد** (الخرطوم
    افتراضياً، دائرة ● معبأة على الخريطة). لا فروع ثانوية؛ بقية الولايات تظهر
    كـ«حيث نعمل» (○) اعتماداً على سجل المشاريع.
- الحالة المرئية: `منفذ / نشط / مُخطط` (أخضر / أزرق / رمادي).
- عند غياب Supabase يعرض اللوحة إشعار الإعداد؛ والموقع يستعرض **بيانات تجريبية**
  حتى يُربط.

---

## 7. قاعدة البيانات (Supabase)

```sql
tables:    projects, stories, resources, partners, site_stats, state_presence
RLS:       قراءة عامة للكل، كتابة فقط عبر service_role (لا تعدي)
الترحيلات: supabase/migrations/0001_init.sql، 0002_state_presence.sql
```

أدوار المفاتيح:
- `NEXT_PUBLIC_SUPABASE_URL` + `ANON_KEY` — عامة للقراءة (RLS يسمح).
- `SUPABASE_SERVICE_ROLE_KEY` — سري، يستخدم حصرياً داخل `Server Actions` (اللوحة).

---

## 8. الأداء والتحسينات

- **First Load JS** ≈ 103 كيلوبايت مشترك؛ الصفحات العامة ثابتة (Static) أو
  تُعدّ عند الطلب للمشاريع.
- صفحات الخريطة والمشاريع تُبنى عند الطلب (dynamic) لقراءة أحدث البيانات.
- الصور تُحمَّل بروابط (Storage قادم) دون تحميل ثقيل في الإطار الأول.

---

## 9. الخطوات القادمة المقترحة

1. ربط Supabase وتنفيذ `0001_init.sql` + إنشاء مستخدم مدير (`npm run seed`).
2. رفع المشروع إلى GitHub ثم النشر على Vercel (مجاني) مع بيئة المتغيرات.
3. ربط تخزين صور (Supabase Storage) لقصص الميدان ومعارض المشاريع.
4. ✅ **تم** — تطبيق النمط البصري الجديد للخريطة (قسم 3) في `components/SudanMap.tsx`
   (متغيرات `--map-bg/--map-active/--map-inactive/--map-border`، الدوائر المعبأة/المفرغة،
   صندوق المفتاح)، مع جدول `state_presence` وإدارته من الإعدادات.
5. إضافة الصور الحقيقية من الـ PDF والشعارات داخل الواجهة عند الحاجة.
6. تطبيق نظام صفحة-بصفحة (قسم 5 — Page Layouts v2) بالأولويات التالية:
   - الرئيسية: شبكة هيرو 7/5 مع الخريطة المصغّرة داخلها، مجالات العمل كقائمة
     مرقّمة، الأثر كمفتاح خريطة، فاصل خماسي 8px أسفل الهيرو.
   - الغلاف العام: سطر الشريط الخماسي تحت رابط الهيدر النشط، وخريطة الحضور المصغّرة في الفوتر.
   - مشروع واحد: هيرو بخريطة مصغّرة مركزة + شريط أرقام أفقي (نموذج يُعمَّم).
